# Market data is NOT redistributable under Kalshi's terms — never commit it.
# The one exception is data/, which holds identifiers and derived classifications with
# every price, size, volume and rule text stripped; see data/README.md.
*.jsonl
*.jsonl.gz
*.parquet
snapshots/
*.csv
!data/*.csv.gz
!data/*.csv
!data/**/*.csv
!data/**/README.md
!docs/**/*.csv

# Secrets
.env
*.pem
*.key

# Python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/
.DS_Store
