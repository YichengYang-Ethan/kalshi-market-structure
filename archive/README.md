# archive/

The full research log: per-board deep profiles, the exchange-wide census findings,
process documents from four audit rounds, and one-off analysis scripts. Preserved
verbatim for auditability — every number in the maintained surface traces back here.

Nothing in this directory is maintained. The maintained surface is `docs/` (seven
documents), `src/`, the four scripts in `scripts/`, and `data/`.
